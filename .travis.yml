
language: node_js

node_js:
 - stable

branches:
  only:
  - master

before_install:
  - npm config set registry=https://registry.npm.taobao.org/
  - npm i -g yarn
  
install:
  - yarn install

script:
 - yarn run build